name = "wyrdstudios-site"
compatibility_date = "2024-01-01"

[site]
bucket = "./out"

# Cloudflare Pages build settings (reference — actual config is in Pages dashboard)
# Build command: npm run build
# Build output directory: out
# Root directory: /
# Node version: 20
